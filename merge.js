var Git = require('gity');
var fs = require('fs');

/*
 first create a second branch with differences in the README

 second, create a node script that attempts to merge develop
 into master

 third, create a script (check.js) to check that the merge
 conflict has been resolved
*/
